<Window x:Class="AIIDEWPF.Views.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="修改密码" Height="320" Width="380"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="NoResize" Background="Transparent"
        Owner="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}">
    <Border Background="#2d2d2d" CornerRadius="10" BorderBrush="#454545" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="🔑 修改密码" FontSize="18" FontWeight="Bold"
                       Foreground="#e0e0e0" HorizontalAlignment="Center" Margin="0,0,0,16"/>

            <TextBlock Grid.Row="1" Text="旧密码" Foreground="#aaa" FontSize="12" Margin="0,0,0,4"/>
            <PasswordBox x:Name="OldPasswordBox" Grid.Row="2"
                         Background="#3a3a3a" Foreground="#d4d4d4" BorderThickness="0"
                         Padding="10,8" FontSize="14" Height="36"/>

            <TextBlock Grid.Row="3" Text="新密码" Foreground="#aaa" FontSize="12" Margin="0,10,0,4"/>
            <PasswordBox x:Name="NewPasswordBox" Grid.Row="4"
                         Background="#3a3a3a" Foreground="#d4d4d4" BorderThickness="0"
                         Padding="10,8" FontSize="14" Height="36"/>

            <TextBlock Grid.Row="5" x:Name="ErrorText" Foreground="#f44747" FontSize="12"
                       HorizontalAlignment="Center" Margin="0,8,0,4" MinHeight="20"/>

            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="取消" Width="80" Height="34" Background="#555" Foreground="#d4d4d4"
                        BorderThickness="0" FontSize="13" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button Content="确认修改" Width="100" Height="34" Background="#007acc" Foreground="White"
                        BorderThickness="0" FontSize="13" Click="Confirm_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
