<Application x:Class="AIIDEWPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AIIDEWPF"
             xmlns:conv="clr-namespace:AIIDEWPF.Converters">
    <Application.Resources>

        <!-- ===== 全局转换器 ===== -->
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>
        <conv:PercentageToWidthConverter x:Key="PercentageToWidth"/>

        <!-- ===== Global Cursor: Hand for clickable controls ===== -->
        <Style TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.88"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.72"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="Hyperlink">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- TextBox / PasswordBox focus ring (Cursor-style subtle border) -->
        <Style TargetType="TextBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        <!-- ===== Dark MenuItem ControlTemplate ===== -->
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#d4d4d4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="#454545" BorderThickness="0"
                                Cursor="Hand">
                            <Grid>
                                <!-- Submenu popup -->
                                <Popup x:Name="PART_Popup" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                       AllowsTransparency="False" Placement="Right"
                                       PopupAnimation="None"
                                       Focusable="False">
                                    <Border Background="#2d2d2d" BorderBrush="#555" BorderThickness="1"
                                            MinWidth="140">
                                        <ItemsPresenter/>
                                    </Border>
                                </Popup>

                                <!-- Top-level: simple text only -->
                                <ContentPresenter x:Name="TopLevelPresenter"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"
                                                  Margin="6,3,6,3"/>

                                <!-- Submenu: full layout with icon + text + shortcut + arrow -->
                                <Grid x:Name="SubMenuGrid" Visibility="Collapsed" MinHeight="30">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" ContentSource="Icon"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Width="16" Height="16" Margin="6,0,6,0"/>
                                    <TextBlock Grid.Column="1" Text="{TemplateBinding Header}"
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}"
                                               Foreground="#888" VerticalAlignment="Center" Margin="12,0,6,0"/>
                                    <Path Grid.Column="3" x:Name="SubMenuArrow"
                                          Data="M0,0 L4,4 L0,8 Z" Fill="#888"
                                          VerticalAlignment="Center" Margin="2,0,6,0"
                                          Visibility="Collapsed"/>
                                </Grid>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- TopLevel: open downward, show simple presenter, hide submenu grid -->
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                            </Trigger>
                            <Trigger Property="Role" Value="TopLevelItem">
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                            </Trigger>
                            <!-- Submenu: open rightward, show full layout, hide simple presenter -->
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="TopLevelPresenter" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="SubMenuGrid" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuItem">
                                <Setter TargetName="TopLevelPresenter" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="SubMenuGrid" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3a3a3a"/>
                                <Setter TargetName="Border" Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#d4d4d4"/>
            <Setter Property="BorderBrush" Value="#555"/>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="#454545"/>
        </Style>

        <!-- Dark ComboBox drop-down -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#3a3a3a"/>
            <Setter Property="Foreground" Value="#d4d4d4"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          Foreground="{TemplateBinding Foreground}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="1" Padding="7,5"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False"
                                          ClickMode="Press" Cursor="Hand">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="CbbBorder" Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1" Padding="7,4" CornerRadius="4">
                                            <Path Data="M0,0 L4,4 L8,0 Z" Fill="#888"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center" Margin="0,0,2,0"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CbbBorder" Property="Opacity" Value="0.85"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              VerticalAlignment="Center"
                                              Margin="8,0,24,0"
                                              IsHitTestVisible="False"/>
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom"
                                   AllowsTransparency="False"
                                   Focusable="False"
                                   PopupAnimation="None">
                                <Border Background="#2d2d2d" BorderBrush="#555" BorderThickness="1"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#d4d4d4"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3a3a3a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tight menu bar layout -->
        <Style TargetType="Menu">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Clickable items: ListBoxItem / TreeViewItem / Expander / TabItem -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="Expander">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Application.Resources>
</Application>
